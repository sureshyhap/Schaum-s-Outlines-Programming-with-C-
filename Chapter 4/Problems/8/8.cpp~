#include <iostream>

int main() {
  std::cout << "Enter a number of squares: ";
  int n = 0;
  std::cin >> n;

  for (int i = 1, sum = 0; i <= n; ++i) {
    sum += i * i;
  }

  std::cout << "The sum is " << sum << ".";
  
  return 0;
}
